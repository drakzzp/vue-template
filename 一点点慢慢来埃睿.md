next
    为什么要用next，用的是什么版本的next  13
    csr和ssr是什么  ssr是服务器端渲染  csr是客户端渲染
    next有哪些策略的渲染

    SSR 服务器渲染
    客户端接收html后下载JavaScript进行水合（hydration）

    SSG 静态站点生成
    构建阶段预先生成所有页面的 HTML，部署至 CDN等服务器。适用于内容相对稳定的场景，如博客文章、产品介绍页面。
    该方式具有极快的访问速度，服务器负载极低。

    ISR 增量静态再生
    SSG 的增强版本。页面以静态形式存在，内容更新时在后台异步重新生成，无需重新部署。这是处理"低频更新内容"场景
    的最优方案。
    懂了，html是静态的，一开始访问都是这个，超出设置的revalidate时间后，下个访问用户，会先命中老的缓存页面
    也就是无等待无白屏，服务器会在后台异步拉取最新数据，生成新静态页面覆盖缓存

    文件系统即是路由

    APPRouter和PageRouter
    1. Pages：所有组件默认客户端组件
    2. App：layout/page 默认【RSC 服务端组件】（跑在 Node，不打包 JS 到浏览器）
    需要useState/useEffect/点击事件加"use client"才变成客户端组件；
    取消了 getStaticProps/getServerSideProps，用 fetch 配置控制渲染

    server Component出现的意义是为了更靠近服务器端用来获取数据，
    
    client component如何用server component的缓存

    可以通过props传递数据
    用 Server Action 封装数据请求
    创建 Route Handler 接口

ai agent 的面试题
你设计过rag系统吗，讲一下chunk策略怎么选的
Chunking 策略决策树

react diff算法
React Diff 为降低复杂度做三层假设：只同层对比、type 相同才复用、key 区分节点。
单节点先对比 type、key，一致则复用 DOM 更新属性；
列表 Diff 先用头尾双指针匹配无需移动的节点，剩余节点通过 key 建立映射，借助最长递增子序列减少 DOM 移动；key 不能用索引，否则会丢失节点复用能力。
React16/18 Diff 比对算法本身无改动，仅 18 支持分片中断调度。

突然想到的问题
    虚拟列表
    ssr原理
    水合的考点
