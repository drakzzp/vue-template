tcl 外包

项目是不是自己从零到一搭建的
有没有做兼容 umd 兼容

UMD 原生内置了 CommonJS、AMD、浏览器全局变量三种模块规范的兼容逻辑，所以我们平台通过 script 动态引入 CDN 上的 umd 组件包时，组件会自动挂载到 window 供画布调用。但 UMD 仅解决模块导入规范问题，无法兼容低版本浏览器 ES6 语法，因此组件工厂打包流程中会集成 Babel 将代码转译为 ES5，并按需注入 polyfill，同时统一规范每个组件挂载到 window 的全局变量名，解决多组件命名冲突问题，实现全场景兼容。

websocket比如连接中断了要怎么办

针对跨屏联动 WebSocket 断连问题，我们四层容错处理：
实现 3 秒一次心跳检测，快速识别网络断开，避免长时间无效连接；
断开后采用指数退避自动重连，延时逐步递增并设置上限，防止高频重连压垮服务；
断连期间缓存所有用户联动操作，重连成功后批量补发，保证多终端数据同步不丢失；
页面销毁时主动关闭 socket 并清空定时器，同时增加业务降级，长时间重连失败则关闭跨屏联动，保障大屏基础可视化功能可用。

有没有做过离线
第一类是跨屏联动 WebSocket 临时离线处理，断网时缓存用户所有联动操作至内存队列，网络恢复重连后批量补发，保证多终端状态同步，同时限制队列长度避免性能问题；
第二类是整套大屏完整离线可用方案，一是支持导出离线静态资源包，打包依赖组件、静态数据，无网络本地直接打开；二是基于 PWA+IndexedDB 实现浏览器缓存离线，持久化大屏配置与离线操作，联网后同步数据。离线场景下会自动降级关闭跨屏联动功能，保障基础可视化查看不受影响，适配油田厂区无外网的业务场景。

第一家八股文

第二家实际场景题

数组如何变成树状结构
货币转换
react diff算法，key的作用，没有key用什么，用索引

问了一个登录鉴权
用户访问：用户尝试访问某个应用系统（如系统 A），但此时用户尚未登录。系统 A 检测到用户未登录，将用户重定向到统一认证中心（SSO Server）。
认证过程：统一认证中心验证用户身份，这可能通过用户名密码、第三方认证（如微信、QQ 登录）等方式。若用户首次登录，需在认证中心进行注册和认证。认证成功后，认证中心会为用户生成一个票据（Ticket），这是用户身份的凭证。
票据传递：认证中心将包含票据的重定向地址返回给系统 A。系统 A 收到后，携带票据向认证中心验证票据的有效性。
访问授权：认证中心验证票据无误后，告知系统 A 该用户已通过认证，系统 A 确认后允许用户访问。同时，在用户后续访问其他应用系统（如系统 B）时，系统 B 同样检测到用户未登录，重定向到认证中心。认证中心发现用户已登录，直接返回包含有效票据的重定向地址给系统 B，系统 B 验证票据后授权用户访问。

SSO = Single Sign-On 单点登录
不跨顶级域名 后端存sessionId在redis/数据库里，前端cookie带上这个sessionId
通过请求携带的 sessionId，去后端会话存储（Redis / 数据库 / 内存）查找对应的有效会话数据

跨顶级域名

浏览器通过 Cookie（默认 connect.sid）携带 sessionId；
express-session 中间件读取 Cookie 里的 sid，用 secret 校验签名防篡改；
用 sid 作为 key 查询外部会话存储（Redis / 数据库）读取会话数据，同时校验过期；
校验通过后挂载 req.session，通过判断 req.session 是否存在登录信息确认登录状态；
可手动取出 req.session.id，直接调用存储 API 做额外校验。


轻量同域 SSO：搭建 Node 统一认证中心，设置主域 Cookie / 全局密钥 JWT，子应用跳转统一登录、共享身份凭证
标准跨域 SSO：用 Node 搭建 OIDC IdP（oidc-provider）或对接成熟 OIDC 服务，走 OAuth2 授权码模式下发 ID Token，Node 子服务校验标准 JWT 身份凭证实现跨域单点登录
核心：统一认证中心 + 标准身份凭证（OIDC JWT）+ 跳转授权模式，区别于单系统本地 JWT/Session 鉴权


ssr我也没有精准回答上，我为什么不说出来啊，搞得好像不懂一样

知识库怎么分片
清洗：去除乱码 / 重复 / 页眉页脚 / 敏感信息、统一编码格式、修正排版，做基础文本标准化，保留核心知识点
切块：优先按 Markdown 章节 / 语义边界分割，设置合理 chunk size + overlap，保证语义完整性，附加元数据标签
校验去重：过滤无效碎片块，优化块粒度，最后 embedding 入库做向量检索

​文档加载​：从本地文件或网页加载文档
​文本分割​：将文档分割为可管理的 chunks
​向量存储​：将 chunks 转换为向量并存储
​检索​：根据用户查询检索相关 chunks
​生成​：基于检索结果生成回复

文档类型是什么?
代码文件 按函数/类分块(保留缩进结构)
技术文档(Markdown) -按标题层级分块(保留树结构)
PDF 长文-按段落+语义边界分块
对话记录 -按对话轮次分块
结构化数据(JS0N) 按实体/记录分块

qiankun状态管理  globalState
react16/18 diff算法区别

agent是什么
AI Agent 是以大模型（LLM）为认知大脑，配套记忆、任务规划、工具调用、自我反思模块，形成感知 - 思考 - 行动 - 反馈 - 复盘完整闭环，能自主拆解复杂目标、主动调用外部能力、持续迭代直至交付结果的智能系统。

文字转语音大概逻辑
语音采集 → ASR 语音转文字 → Agent 逻辑调度 → LLM 思考生成回答 → TTS 文字转语音 → 音频播放

有封装过什么组件
react 

怎么连接的数据库
是用Sequelize + mysql2 连接的数据库

怎么复制表
直接复制不会复制主键、索引、自增、外键，只复制字段和数据。
-- 1. 先复制完整结构
CREATE TABLE user_copy LIKE user;
-- 2. 再插入所有数据
INSERT INTO user_copy SELECT * FROM user;

它这个增强提示词的功能
分支判断：
轻量模式：走规则词典匹配，同步计算直接返回结果
LLM 模式：组装系统提示词 + 用户原文，调用大模型推理接口，异步等待返回

发现页面掉帧的话如何去排查问题
1. Chrome Performance 面板（最核心）
2. React DevTools Profiler（定位组件渲染耗时）
3. Layers / Rendering 面板（排查渲染层、重绘）

浏览器缓存有哪些
一、HTTP 缓存（服务器 + 浏览器协同，最核心资源缓存）
二、浏览器本地持久化存储（前端 JS 手动操作，永久 / 长期缓存）
1. Cookie
用途：登录态、简单标识
可设置Expires/Max-Age过期；
2. localStorage
5MB 左右
共享规则：同源（协议 + 域名 + 端口一致）所有打开的标签、新窗口都共用一份存储
同步机制：一个标签修改 localStorage，其他同源标签会触发 storage 事件收到更新
生命周期：关闭浏览器不会清除，永久保存，手动清除缓存 / 代码删除才消失
3. sessionStorage
仅当前标签页生命周期，关闭标签立即清空
4. IndexedDB
几十 MB～硬盘配额（很大）
特点：异步数据库，存对象、二进制（图片 / 文件）；同源；适合大量离线数据。
用途：复杂离线缓存、大文件本地存储

三、内存缓存（Memory Cache）
存储位置：浏览器内存
生命周期：页面关闭 / 刷新直接清空
缓存内容：当前页面加载过的图片、脚本、字体等静态资源
特点：读取速度极快，无 IO 磁盘操作；不持久化。
优先级：比磁盘缓存高，优先读取内存。

四、磁盘缓存（Disk Cache / HTTP 缓存底层载体）
HTTP 强缓存、协商缓存落地存储介质，存在硬盘文件：
生命周期：浏览器关闭依然保留，直到过期 / 手动清除缓存
速度：慢于内存，但持久
存放：所有符合缓存策略的静态资源

五、Service Worker Cache（离线缓存专用）
独立缓存空间，由 Service Worker 脚本控制，属于离线 PWA 核心：

还有个dns缓存

说一下为什么要用react
1. 数据驱动视图，告别手动 DOM 操作
2. 分层解耦，职责清晰
3. ViewModel 可单独单元测试（最大工程价值）
4. 双向绑定简化表单开发
5. 视图与逻辑分离，多人协作友好
6. 状态统一管理，页面状态清晰
7. 复用性强

问那个可视化低代码项目的
json schema是什么结构
组件接受什么形式的props
一行放几个组件

准备汇丰的面试吧
react